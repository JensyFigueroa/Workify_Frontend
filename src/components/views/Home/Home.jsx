import { Cards } from "../../Cards/Cards";
import FilterBar from "../../FilterBar/FilterBar";

const Home = () => {
  return (
    <div>
      <FilterBar />
      <Cards />
    </div>
  );
};

export default Home;
